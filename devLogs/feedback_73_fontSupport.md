for the Export Configruation, in the Overall Settings, add a Font Settings to allow the user to select a font in the system to use for the exported PDF, allow them to select from a drop down list, allow searching, and add variant drop down for light, extra light, bold, condences, etc.

Allow the user to click an upload font button to upload their font to the server.

Alter the Save Layout button to overide the currently selected layout preset if there is one, and if no preset is loaded, then prompt the user to save a new one.
