there are still some flaws in the current agent schedule:

* the agent forget to assign faculty for the courses scheduled

* the agent always add new tables instead of updating existing ones when asked to reschedule.

can you help fixing it?