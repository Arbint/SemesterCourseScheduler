add a user table in the data base. For now, let's allow maximum one user. the user should have a user name and password.

add a login button on the upper right corner, if there is no user yet, click the login button should prompt to create the user. the user should have a username and password.

if logged in, the user can access all features.
if not logged in, everything is visible, the filters and sorting works, but Nothing else is adjustable.


