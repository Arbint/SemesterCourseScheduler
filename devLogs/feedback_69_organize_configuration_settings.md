the configuration settings is all of the place, can you organized it? 

I have write down the desired layout as well as some requested changes as they are commented.

* Saved Layout dropdown and Save Layout Button

* overall settings
    * Orientation
    * Page Size

* Header  (collapsable)
    * Header Text Info Settings

    * Header Image Settings (the D-pad is abit space consuming, can you layout the other setting of the Header image to overlap with the height space it streches? horizontally?)

    * Attribute Image Settings, this only exist in the faculty schedule, let's make sure this one is not limited to less or equal to 3, make it scale up to as big as needed, also add a D-pad control on it. 

* Footer (Collapsable)
    * Footer Image Settings, please give it the same D-pad as the Header image, in otherwords, the footer should have identitcal settings as the header image.

* Table Body:
    * Table Font Size
    * Time label font size
    * Weekday font size

Another thing to note is that the header and footer image and settings should be unique, between the room schedule and faculty schedule, they also should have their own unique settings and saved presets, and not affecting each other.

Allow the user to rename duplicate, and delete presets.



