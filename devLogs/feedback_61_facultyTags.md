some faculty have special attributes, for example, many of our faculty are FLIGHT certified. sof FLIGHT is a possible attribute to be added to them.

in our data base, add a faculty attribute table.

a faculty attribute will have:

* Name
* Icon (the path to the icon of the tag, user should be able to added it in the front end)

faculty should be able to have multiple attributes, alter the database to support that.

In the front end, under the faculty tab, under the faculty list, add a attribute list for the user to add and configure attributes.


