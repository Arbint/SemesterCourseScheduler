Two changes on the schedule table:

* if a course is having a frequency of n, allow it to be dropped on a table that has more than n weekdays, but the course should have toggles in the form of letters of each workdays of the table, and the user has to toggle on exactly n of the weekdays and the others turned off, if not, show a warning in the warning list.

* add a room called online as the last column, an online room can contain as many course as needed (layed out in horizonal way) as long as there are no other conflicts.