# The view page

The Term Schedules tabe is a bit crowded with all the columns.

To be able to show the schedule to people, they only need to see the Schedule Tables.

Create another tab called View, put the tab button between the Load and Change List tab.

The view tab only has the term selection dropdown list, the filters, and the Schedule Tables. not coruse list, issues, or AI Audit.

The unused rooms and Time Slot rows should be hidden.

All the button that would alter the data would be disabled, this should be a view only page.

Add a Font Size spin box next to the term selection dropdown list, allowing the view to change the size of the font used in the table.

Add a get view URL button next to the Font Size spin box, when clicked, It will generate an URL and put it in my clipboard. The URL should embed the infomation of with term is selected.

The URL should leads to the view tab with the embed term selected.